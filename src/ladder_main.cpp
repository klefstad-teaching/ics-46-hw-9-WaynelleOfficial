#include "ladder.h"


int main() {
    // set<string> word_list = {"cat", "chat", "cheat", "car", "care", "cart", "carts"};
    // string begin_word = "car", end_word = "cheat";

    // vector<string> result = generate_word_ladder(begin_word, end_word, word_list);
    // print_word_ladder(result);
    verify_word_ladder();

    return 0;
}